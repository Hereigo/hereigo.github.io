### 2023-12-04

```bash

npm init

npm i mongoose express body-parser #(for json-2-js)

npm i  --save-dev nodemon #(auto-restarter after save)

npm i bcrypt #(for hash-ing passwords, etc. )

mkdir config
echo "const URI = 'mongodb+srv://...';
module.exports = {URI}" >> ./config/db.js


```