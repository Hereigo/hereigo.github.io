import { assert, expect, should } from 'chai';
import { describe, it } from 'mocha';

describe('when', () => {
    it('should', () => {
        expect();
        should();
        assert();
    })
});